<Window x:Class="WpfClient.DodajNaListuZeljaProzor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dodavanje knjige"
        Width="450" Height="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="Resources/bookshelf.ico">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" x:Name="listKnjige"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ISBN"  Binding="{Binding ISBN}"   Width="80"/>
                <DataGridTextColumn Header="Naziv" Binding="{Binding Naziv}"  Width="*"/>
                <DataGridTextColumn Header="Žanr"  Binding="{Binding Zanr}"   Width="90"/>
                <DataGridTextColumn Header="Cena"  Binding="{Binding Cena}"   Width="65"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Odustani" Width="80" Margin="0,0,8,0"
                    Click="BtnOdustani_Click"/>
            <Button Content="Dodaj"    Width="80"
                    Click="BtnDodaj_Click"/>
        </StackPanel>
    </Grid>
</Window>